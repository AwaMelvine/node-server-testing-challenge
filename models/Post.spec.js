const db = require('../data/dbConfig');
const Post = require('../models/Post');

describe('Post Model', () => {
    describe('get()', () => {
        it('returns json ', () => {

        });
        it('returns an array when no arguments supplied', () => {

        });
        it('returns object when id is specified', () => {

        });
    });

    describe('add()', () => {
        it('creates a post', () => {

        });
        it('returns an object', () => {

        });
    });

    describe('remove()', () => {
        it('deletes a post', () => {

        });
    });
});