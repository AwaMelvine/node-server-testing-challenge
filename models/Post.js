const db = require('../data/dbConfig');

module.exports = {
    async get(id = null) {
        return null;
    },

    async add(post) {
        return null;
    },

    async remove(id) {
        return null;
    }
}